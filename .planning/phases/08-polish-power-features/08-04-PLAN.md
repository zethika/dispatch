---
phase: 08-polish-power-features
plan: 04
type: execute
wave: 2
depends_on:
  - "08-02"
  - "08-03"
files_modified:
  - src/App.tsx
  - src/App.test.tsx
  - src/features/shortcuts/ShortcutCheatsheet.tsx
autonomous: true
requirements:
  - KEY-01
  - KEY-02
  - KEY-03
  - KEY-04
  - KEY-05
  - KEY-06
  - KEY-07
  - KEY-08

must_haves:
  truths:
    - "Cmd+Enter sends the current request from anywhere in the app"
    - "Cmd+N creates a new request in the active collection"
    - "Cmd+Shift+N creates a new collection"
    - "Cmd+K opens the search modal"
    - "Cmd+E cycles to the next environment"
    - "Cmd+Shift+C copies the current request as cURL to clipboard"
    - "Cmd+W closes/deselects the current request"
    - "Cmd+S triggers a sync push"
    - "Cmd+/ opens the keyboard shortcut cheatsheet"
    - "All shortcuts fire even when focus is in a text input (D-10)"
    - "Shortcut flash feedback appears on the target element (D-12)"
  artifacts:
    - path: "src/features/shortcuts/ShortcutCheatsheet.tsx"
      provides: "Cmd+/ shortcut reference modal"
      contains: "Keyboard Shortcuts"
    - path: "src/App.tsx"
      provides: "Global keydown listener for all shortcuts"
      contains: "metaKey"
  key_links:
    - from: "src/App.tsx"
      to: "src/stores/requestStore.ts sendRequest"
      via: "Cmd+Enter handler calls sendRequest"
      pattern: "sendRequest"
    - from: "src/App.tsx"
      to: "src/stores/uiStore.ts setSearchOpen"
      via: "Cmd+K handler opens search"
      pattern: "setSearchOpen"
    - from: "src/App.tsx"
      to: "src/utils/curl.ts buildCurlString"
      via: "Cmd+Shift+C handler exports cURL"
      pattern: "buildCurlString"
---

<objective>
Implement all global keyboard shortcuts and the shortcut cheatsheet modal.

Purpose: Power users can control the app entirely via keyboard (KEY-01 through KEY-08) per D-10/D-11/D-12.
Output: Global keydown listener in App.tsx, ShortcutCheatsheet modal, flash feedback on shortcut targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-power-features/08-CONTEXT.md
@.planning/phases/08-polish-power-features/08-RESEARCH.md
@.planning/phases/08-polish-power-features/08-UI-SPEC.md
@.planning/phases/08-polish-power-features/08-02-SUMMARY.md
@.planning/phases/08-polish-power-features/08-03-SUMMARY.md

<interfaces>
<!-- Key types the executor needs from prior plans -->

From src/utils/curl.ts (created in Plan 02):
```typescript
export function buildCurlString(
  method: string, url: string, headers: KeyValueEntry[],
  body: RequestBody | null, auth: RequestAuth | null,
  vars: Record<string, string>
): string;
```

From src/stores/uiStore.ts (extended in Plan 03):
```typescript
interface UiState {
  splitRatio: number; setSplitRatio: (ratio: number) => void;
  searchOpen: boolean; setSearchOpen: (open: boolean) => void;
  cheatsheetOpen: boolean; setCheatsheetOpen: (open: boolean) => void;
}
```

From src/stores/requestStore.ts:
```typescript
interface RequestStore {
  method: string; url: string; headers: KeyValueEntry[];
  body: RequestBody | null; auth: RequestAuth | null;
  sendRequest: () => Promise<void>;
  clearActiveRequest: () => void;
  activeRequestMeta: ActiveRequestMeta | null;
}
```

From src/stores/collectionStore.ts:
```typescript
interface CollectionStore {
  workspaceId: string | null;
  collections: CollectionItem[];
  activeRequestId: string | null;
  createRequest: (collectionSlug: string, parentPath: string[], name: string) => Promise<void>;
  createCollection: (name: string) => Promise<void>;
  clearActiveRequest: () => void;
}
```

From src/stores/environmentStore.ts:
```typescript
// Has activeEnvVariables getter and environments array
// Has setActiveEnvironment action
```

From src/stores/syncStore.ts:
```typescript
// Has triggerPush action
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShortcutCheatsheet modal</name>
  <files>
    src/features/shortcuts/ShortcutCheatsheet.tsx
  </files>
  <read_first>
    src/stores/uiStore.ts,
    src/features/http/CurlImportModal.tsx
  </read_first>
  <action>
Create `src/features/shortcuts/ShortcutCheatsheet.tsx`.

HeroUI Modal per D-11 and UI-SPEC. Title: "Keyboard Shortcuts". Two-column grid layout showing all shortcuts.

```tsx
import { Modal, ModalContent, ModalHeader, ModalBody } from '@heroui/react';
import { useUiStore } from '../../stores/uiStore';

const SHORTCUTS = [
  { keys: 'Cmd + Enter', label: 'Send request' },
  { keys: 'Cmd + N', label: 'New request' },
  { keys: 'Cmd + Shift + N', label: 'New collection' },
  { keys: 'Cmd + K', label: 'Search' },
  { keys: 'Cmd + E', label: 'Switch environment' },
  { keys: 'Cmd + Shift + C', label: 'Copy as cURL' },
  { keys: 'Cmd + W', label: 'Close request' },
  { keys: 'Cmd + S', label: 'Force sync' },
  { keys: 'Cmd + /', label: 'This cheatsheet' },
];
```

Each row: label on left (`text-sm`), key badge on right.
Key badge styling per UI-SPEC: `<kbd className="text-xs font-semibold font-mono bg-primary/10 text-primary px-2 py-0.5 rounded">`.

Modal props: `isOpen={cheatsheetOpen}`, `onClose={() => setCheatsheetOpen(false)}`, `size="md"`.
  </action>
  <verify>
    <automated>cd /Users/kennethfabianberg/Documents/rust/dispatch && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/features/shortcuts/ShortcutCheatsheet.tsx exists
    - File contains `Keyboard Shortcuts` (modal title)
    - File contains `Cmd + Enter` and `Send request`
    - File contains `Cmd + K` and `Search`
    - File contains `Cmd + /` and `This cheatsheet`
    - File contains `bg-primary/10 text-primary` (key badge styling)
    - File contains `useUiStore`
    - `npx tsc --noEmit` succeeds
  </acceptance_criteria>
  <done>ShortcutCheatsheet modal renders all 9 shortcuts with proper styling</done>
</task>

<task type="auto">
  <name>Task 2: Global keyboard shortcut listener in App.tsx with flash feedback</name>
  <files>
    src/App.tsx,
    src/App.test.tsx
  </files>
  <read_first>
    src/App.tsx,
    src/App.test.tsx,
    src/stores/requestStore.ts,
    src/stores/collectionStore.ts,
    src/stores/environmentStore.ts,
    src/stores/syncStore.ts,
    src/stores/uiStore.ts,
    src/utils/curl.ts
  </read_first>
  <action>
**Step 1: Add a `flashElement` utility function** (can be at top of App.tsx or in a utils file).

```typescript
function flashElement(shortcutId: string) {
  const el = document.querySelector(`[data-shortcut-id="${shortcutId}"]`);
  if (!el) return;
  el.classList.add('ring-2', 'ring-primary', 'ring-offset-1', 'rounded');
  setTimeout(() => {
    el.classList.remove('ring-2', 'ring-primary', 'ring-offset-1', 'rounded');
  }, 300);
}
```

Per D-12: 0ms delay, 300ms duration, no easing.

**Step 2: Add the global keydown `useEffect` in App.tsx.**

This follows the existing pattern of the focus-pull `useEffect` already in App.tsx. Use `.getState()` to read stores (not hooks) — this allows the empty `[]` dependency array per RESEARCH.md Pattern 5.

Per D-10: do NOT check `event.target` for input/textarea suppression. All shortcuts fire regardless of focus.

```typescript
useEffect(() => {
  const handler = (e: KeyboardEvent) => {
    // Cmd+Enter — send request (KEY-01)
    if (e.metaKey && !e.shiftKey && e.key === 'Enter') {
      e.preventDefault();
      void useRequestStore.getState().sendRequest();
      flashElement('send-button');
      return;
    }
    // Cmd+N — new request (KEY-02)
    if (e.metaKey && !e.shiftKey && e.key === 'n') {
      e.preventDefault();
      const { collections, activeRequestId } = useCollectionStore.getState();
      // Determine active collection from activeRequestId (first segment) or use first collection
      const collSlug = activeRequestId?.split('/')[0] ?? collections[0]?.slug;
      if (collSlug) {
        void useCollectionStore.getState().createRequest(collSlug, [], 'New Request');
      }
      flashElement('new-request');
      return;
    }
    // Cmd+Shift+N — new collection (KEY-03)
    if (e.metaKey && e.shiftKey && e.key === 'N') {
      e.preventDefault();
      void useCollectionStore.getState().createCollection('New Collection');
      flashElement('sidebar-area');
      return;
    }
    // Cmd+K — open search (KEY-04)
    if (e.metaKey && !e.shiftKey && e.key === 'k') {
      e.preventDefault();
      useUiStore.getState().setSearchOpen(true);
      flashElement('search-icon');
      return;
    }
    // Cmd+E — cycle environment (KEY-05)
    if (e.metaKey && !e.shiftKey && e.key === 'e') {
      e.preventDefault();
      const envStore = useEnvironmentStore.getState();
      const envs = envStore.environments;
      if (envs.length > 0) {
        const currentSlug = envStore.activeEnvironmentSlug;
        const currentIdx = envs.findIndex((env) => env.slug === currentSlug);
        const nextIdx = (currentIdx + 1) % envs.length;
        envStore.setActiveEnvironment(envs[nextIdx].slug);
        // Import toast at top of file: import { toast } from 'sonner';
        toast.success(`Environment: ${envs[nextIdx].name}`);
      }
      flashElement('env-selector');
      return;
    }
    // Cmd+Shift+C — copy as cURL (KEY-06)
    if (e.metaKey && e.shiftKey && e.key === 'C') {
      e.preventDefault();
      const req = useRequestStore.getState();
      if (req.activeRequestMeta) {
        const vars = useEnvironmentStore.getState().activeEnvVariables;
        const curlStr = buildCurlString(req.method, req.url, req.headers, req.body, req.auth, vars);
        void navigator.clipboard.writeText(curlStr);
        toast.success('Copied as cURL');
      }
      flashElement('url-bar');
      return;
    }
    // Cmd+W — close request (KEY-07)
    if (e.metaKey && !e.shiftKey && e.key === 'w') {
      e.preventDefault();
      useCollectionStore.getState().setActiveRequest(null);
      flashElement('active-tab');
      return;
    }
    // Cmd+S — force sync (KEY-08)
    if (e.metaKey && !e.shiftKey && e.key === 's') {
      e.preventDefault();
      void useSyncStore.getState().triggerPush();
      flashElement('sync-chip');
      return;
    }
    // Cmd+/ — cheatsheet (D-11)
    if (e.metaKey && e.key === '/') {
      e.preventDefault();
      useUiStore.getState().setCheatsheetOpen(true);
      return;
    }
  };

  window.addEventListener('keydown', handler);
  return () => window.removeEventListener('keydown', handler);
}, []);
```

Add required imports at the top of App.tsx:
```typescript
import { toast } from 'sonner'; // already imported via Toaster, but add if not imported for toast calls
import { useRequestStore } from './stores/requestStore';
import { useEnvironmentStore } from './stores/environmentStore';
import { useUiStore } from './stores/uiStore';
import { buildCurlString } from './utils/curl';
```

Note: `useCollectionStore`, `useSyncStore` are already imported.

**Step 3: Render the ShortcutCheatsheet modal in App.tsx.**

```tsx
import ShortcutCheatsheet from './features/shortcuts/ShortcutCheatsheet';

// In the return JSX, after the Toaster:
<ShortcutCheatsheet />
```

The ShortcutCheatsheet reads `cheatsheetOpen` from `uiStore` internally.

**Step 4: Add `data-shortcut-id` attributes to target elements.**

These need to be added to the appropriate components. Since the executor reads those files, add them:
- In `UrlBar.tsx`: add `data-shortcut-id="send-button"` to the Send button element and `data-shortcut-id="url-bar"` to the URL bar container
- In `TopBar.tsx`: add `data-shortcut-id="search-icon"` to the search trigger, `data-shortcut-id="env-selector"` to the environment dropdown trigger, `data-shortcut-id="sync-chip"` to the SyncStatusChip wrapper
- In `Sidebar.tsx` or the sidebar container: add `data-shortcut-id="sidebar-area"` to the sidebar root

**Step 5: Update `src/App.test.tsx`.**

Read the existing test file first. Extend with tests for the keyboard shortcuts. Since the keydown listener uses `.getState()`, mock the stores:

```typescript
import { vi } from 'vitest';

// Test that Cmd+K opens search
it('Cmd+K sets searchOpen to true', () => {
  // Mock useUiStore.getState to return a spy for setSearchOpen
  // Fire keydown event with metaKey=true, key='k'
  // Assert setSearchOpen was called with true
});
```

Add 2-3 key shortcut tests. Don't test all 9 — test the pattern with 2-3 representative shortcuts (Cmd+K, Cmd+Enter, Cmd+S).

Read the existing App.test.tsx mocking patterns and follow them exactly.

**Step 6: Check for environmentStore interface compatibility.**

The Cmd+E handler references `envStore.environments` and `envStore.activeEnvironmentSlug` and `envStore.setActiveEnvironment()`. Read `src/stores/environmentStore.ts` and verify these field names match. If the actual field names differ, use the actual names from the store.
  </action>
  <verify>
    <automated>cd /Users/kennethfabianberg/Documents/rust/dispatch && npx tsc --noEmit 2>&1 | tail -5 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `e.metaKey && !e.shiftKey && e.key === 'Enter'`
    - src/App.tsx contains `e.metaKey && !e.shiftKey && e.key === 'n'`
    - src/App.tsx contains `e.metaKey && e.shiftKey && e.key === 'N'`
    - src/App.tsx contains `e.metaKey && !e.shiftKey && e.key === 'k'`
    - src/App.tsx contains `e.metaKey && !e.shiftKey && e.key === 'e'`
    - src/App.tsx contains `e.metaKey && e.shiftKey && e.key === 'C'`
    - src/App.tsx contains `e.metaKey && !e.shiftKey && e.key === 'w'`
    - src/App.tsx contains `e.metaKey && !e.shiftKey && e.key === 's'`
    - src/App.tsx contains `e.metaKey && e.key === '/'`
    - src/App.tsx contains `flashElement`
    - src/App.tsx contains `ShortcutCheatsheet`
    - src/App.tsx contains `buildCurlString`
    - src/App.tsx does NOT contain `tagName` or `nodeName` check (D-10 compliance)
    - `npx tsc --noEmit` succeeds
    - `npm run test` passes
  </acceptance_criteria>
  <done>All 9 keyboard shortcuts work globally including inside text fields, flash feedback triggers on target elements, cheatsheet modal opens with Cmd+/</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run test` passes (all tests including new shortcut tests)
- Manual: test each shortcut (Cmd+Enter sends, Cmd+N creates request, Cmd+Shift+N creates collection, Cmd+K opens search, Cmd+E cycles env, Cmd+Shift+C copies cURL, Cmd+W closes request, Cmd+S syncs, Cmd+/ opens cheatsheet)
- Manual: verify shortcuts work from inside a text input (D-10)
- Manual: verify flash feedback on target elements (D-12)
</verification>

<success_criteria>
- KEY-01 through KEY-08: All keyboard shortcuts functional from anywhere in the app
- D-10: Shortcuts fire from text fields without suppression
- D-11: Cmd+/ opens shortcut cheatsheet modal
- D-12: Flash feedback ring appears on target element for 300ms
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-power-features/08-04-SUMMARY.md`
</output>
