---
phase: 03-http-engine
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/layout/RequestEditor.tsx
  - src/features/http/KeyValueEditor.tsx
  - src/features/http/BodyEditor.tsx
  - src/features/http/AuthEditor.tsx
  - src/features/http/UrlBar.tsx
autonomous: false
requirements: [HTTP-01, HTTP-02, HTTP-03, HTTP-04, HTTP-05, HTTP-06, HTTP-07]

must_haves:
  truths:
    - "User can select HTTP method (GET, POST, PUT, DELETE) from a dropdown"
    - "User can enter a URL with {{var}} tokens visually distinct"
    - "User can add, toggle, and delete query parameters via key-value editor"
    - "User can add, toggle, and delete headers via key-value editor"
    - "User can write a JSON body in a textarea (body tab always visible per D-03)"
    - "User can set a bearer token in the Auth tab"
    - "User can click Send to execute the request"
    - "Send button shows loading/disabled state during request execution"
  artifacts:
    - path: "src/features/http/KeyValueEditor.tsx"
      provides: "Reusable key-value row editor for params and headers"
      contains: "KeyValueEditor"
    - path: "src/features/http/BodyEditor.tsx"
      provides: "JSON body textarea"
      contains: "BodyEditor"
    - path: "src/features/http/AuthEditor.tsx"
      provides: "Bearer token input"
      contains: "AuthEditor"
    - path: "src/features/http/UrlBar.tsx"
      provides: "Method dropdown + URL input + Send button row"
      contains: "UrlBar"
    - path: "src/components/layout/RequestEditor.tsx"
      provides: "Full request editor with tabs wiring all sub-components"
      contains: "RequestEditor"
  key_links:
    - from: "src/components/layout/RequestEditor.tsx"
      to: "src/stores/requestStore.ts"
      via: "useRequestStore hook"
      pattern: "useRequestStore"
    - from: "src/components/layout/RequestEditor.tsx"
      to: "src/stores/collectionStore.ts"
      via: "activeRequestId subscription for loading request file"
      pattern: "useCollectionStore"
    - from: "src/features/http/UrlBar.tsx"
      to: "src/stores/requestStore.ts"
      via: "sendRequest action"
      pattern: "sendRequest"
---

<objective>
Build the full request editor UI: URL bar with method dropdown and Send button, tab strip (Params | Headers | Body | Auth), KeyValueEditor for params/headers, BodyEditor textarea, and AuthEditor for bearer token. Wire everything to requestStore and trigger request execution on Send click.

Purpose: This is the input side of the HTTP engine — the user configures what to send. Without this, the Rust executor has no way to receive user input.
Output: Fully functional request editor with all tabs, connected to requestStore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-http-engine/03-CONTEXT.md
@.planning/phases/03-http-engine/03-RESEARCH.md
@.planning/phases/03-http-engine/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/stores/requestStore.ts:
```typescript
export type ResponseState =
  | { status: 'idle' }
  | { status: 'loading' }
  | { status: 'success'; data: HttpResponse }
  | { status: 'error'; message: string };

export const useRequestStore = create<RequestStore>((set, get) => ({
  method: string;
  url: string;
  headers: KeyValueEntry[];
  queryParams: KeyValueEntry[];
  body: RequestBody | null;
  auth: RequestAuth | null;
  response: ResponseState;
  activeRequestMeta: { workspaceId: string; collectionSlug: string; parentPath: string[]; slug: string } | null;
  loadFromFile: (file: RequestFile, meta: {...}) => void;
  setMethod: (m: string) => void;
  setUrl: (u: string) => void;
  setHeaders: (h: KeyValueEntry[]) => void;
  setQueryParams: (p: KeyValueEntry[]) => void;
  setBody: (b: RequestBody | null) => void;
  setAuth: (a: RequestAuth | null) => void;
  sendRequest: () => Promise<void>;
}));
```

From src/api/http.ts:
```typescript
export async function loadRequest(params: {
  workspaceId: string; collectionSlug: string; parentPath: string[]; slug: string;
}): Promise<RequestFile>;
export async function saveRequest(params: {
  workspaceId: string; collectionSlug: string; parentPath: string[]; slug: string; request: RequestFile;
}): Promise<void>;
```

From src/stores/collectionStore.ts:
```typescript
activeRequestId: string | null; // slug path: "collection-slug/folder/request-slug"
workspaceId: string | null;
```

From src/types/collections.ts:
```typescript
export interface KeyValueEntry { key: string; value: string; enabled: boolean; }
export interface RequestBody { type: string; content: string; }
export interface RequestAuth { type: string; token: string; }
export interface RequestFile { name: string; method: string; url: string; headers: KeyValueEntry[]; queryParams: KeyValueEntry[]; body: RequestBody | null; auth: RequestAuth | null; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature components (UrlBar, KeyValueEditor, BodyEditor, AuthEditor) and RequestEditor wiring</name>
  <files>
    src/features/http/UrlBar.tsx,
    src/features/http/KeyValueEditor.tsx,
    src/features/http/BodyEditor.tsx,
    src/features/http/AuthEditor.tsx,
    src/components/layout/RequestEditor.tsx
  </files>
  <read_first>
    src/components/layout/RequestEditor.tsx,
    src/stores/requestStore.ts,
    src/stores/collectionStore.ts,
    src/api/http.ts,
    src/types/collections.ts,
    src/components/layout/RightPanel.tsx,
    src/components/CollectionTree/CollectionTree.tsx
  </read_first>
  <action>
    **1. Create `src/features/http/UrlBar.tsx` (per D-02, D-14, D-16):**
    - Props: none (reads from requestStore directly)
    - Layout: single row with `flex gap-2 items-center`
    - **Method dropdown (left):** HeroUI `Dropdown` + `DropdownTrigger` + `DropdownMenu` with methods `['GET', 'POST', 'PUT', 'DELETE']`. Method colors matching Phase 2 convention: GET=`text-success` (green), POST=`text-blue-500`, PUT=`text-warning` (yellow), DELETE=`text-danger` (red). Selected method from `useRequestStore(s => s.method)`, onChange calls `setMethod`.
    - **URL input (center, flex-grow):** HeroUI `Input` with `size="sm"`, `className="flex-1"`, `placeholder="Enter request URL..."`. Value from `useRequestStore(s => s.url)`, onChange calls `setUrl`. For HTTP-02: render `{{var}}` tokens distinctly — use a simple approach: CSS styling on the raw input text is not possible with a plain `<input>`, so for Phase 3, simply render the input as-is. The visual distinction for `{{var}}` is best deferred to the environment phase (Phase 4) where variable resolution exists. Document this in the component as a TODO comment: `// TODO Phase 4: add {{var}} visual highlighting overlay`.
    - **Send button (right, per D-16):** HeroUI `Button` with `color="primary"`, `size="sm"`. Text: "Send". `isLoading` prop bound to `useRequestStore(s => s.response.status === 'loading')` (per D-14). `isDisabled` when `url` is empty OR response status is `'loading'`. `onPress` calls `useRequestStore.getState().sendRequest()`.
    - Export default `UrlBar`.

    **2. Create `src/features/http/KeyValueEditor.tsx` (per D-04, D-05):**
    - Props: `{ entries: KeyValueEntry[]; onChange: (entries: KeyValueEntry[]) => void; keyPlaceholder?: string; valuePlaceholder?: string; }`
    - Renders a list of rows, each row contains:
      - HeroUI `Checkbox` (left): `isSelected={entry.enabled}`, `onValueChange` toggles `enabled` field. When disabled, the row has `opacity-50` class.
      - HeroUI `Input` for key: `size="sm"`, `variant="bordered"`, `placeholder={keyPlaceholder || "Key"}`, `value={entry.key}`, onChange updates key in entries array
      - HeroUI `Input` for value: `size="sm"`, `variant="bordered"`, `placeholder={valuePlaceholder || "Value"}`, `value={entry.value}`, onChange updates value in entries array
      - Delete button (X): HeroUI `Button` with `isIconOnly`, `size="sm"`, `variant="light"`, renders "x" text or a simple X character. `onPress` removes this entry from the array.
    - Below the list: an "+ Add" text button (per D-05): `Button` with `variant="light"`, `size="sm"`. `onPress` appends `{ key: '', value: '', enabled: true }` to entries.
    - Export default `KeyValueEditor`.

    **3. Create `src/features/http/BodyEditor.tsx` (per D-06, D-07):**
    - Props: `{ body: RequestBody | null; onChange: (body: RequestBody | null) => void; }`
    - Renders a plain `<textarea>` (NOT CodeMirror/Monaco per D-06). Tailwind classes: `w-full h-full min-h-[120px] bg-content2 text-foreground font-mono text-sm p-3 rounded-lg border border-default-200 focus:border-primary focus:outline-none resize-y`.
    - Value: `body?.content || ''`. OnChange: calls `onChange({ type: 'json', content: e.target.value })`. If content is empty string, call `onChange(null)`.
    - Placeholder: `"{\n  \"key\": \"value\"\n}"`
    - Phase 3 scope: plain textarea only. JSON syntax highlighting in the body input is Claude's discretion — for Phase 3, skip the input-side highlighting overlay (complex for a textarea) and focus highlighting effort on the response viewer (Plan 03). Add a TODO comment: `// TODO: CSS-based JSON highlighting overlay for body input`.
    - Export default `BodyEditor`.

    **4. Create `src/features/http/AuthEditor.tsx` (per HTTP-06):**
    - Props: `{ auth: RequestAuth | null; onChange: (auth: RequestAuth | null) => void; }`
    - Renders a single section labeled "Bearer Token":
      - A `<p>` label: "Token" with `text-sm text-default-500 mb-1`
      - HeroUI `Input` with `size="sm"`, `variant="bordered"`, `placeholder="Enter bearer token..."`, `type="password"` (tokens are sensitive).
      - Value: `auth?.token || ''`. OnChange: if value is non-empty, call `onChange({ type: 'bearer', token: value })`. If empty, call `onChange(null)`.
    - Export default `AuthEditor`.

    **5. Rewrite `src/components/layout/RequestEditor.tsx` (per D-01):**
    - Replace the entire current shell content.
    - Import: `UrlBar` from `../../features/http/UrlBar`, `KeyValueEditor` from `../../features/http/KeyValueEditor`, `BodyEditor` from `../../features/http/BodyEditor`, `AuthEditor` from `../../features/http/AuthEditor`, `useRequestStore` from `../../stores/requestStore`, `useCollectionStore` from `../../stores/collectionStore`, `Tabs, Tab` from `@heroui/react`, `useEffect, useRef` from `react`, `loadRequest, saveRequest` from `../../api/http`.
    - **Active request loading:** Add a `useEffect` that watches `collectionStore.activeRequestId` and `collectionStore.workspaceId`. When `activeRequestId` changes and is not null:
      - Parse `activeRequestId` string: split by `/` — first segment is `collectionSlug`, last segment is `slug`, middle segments are `parentPath[]`.
      - Call `loadRequest({ workspaceId, collectionSlug, parentPath, slug })`, then call `requestStore.loadFromFile(result, { workspaceId, collectionSlug, parentPath, slug })`.
      - Wrap in try/catch; on error, log to console.error.
    - **Debounced auto-save:** Add a `useEffect` that watches the draft fields (method, url, headers, queryParams, body, auth) and `activeRequestMeta`. Use a `setTimeout` with 500ms delay (debounce). When the timer fires, if `activeRequestMeta` is not null, call `saveRequest({ ...activeRequestMeta, request: { name: '(loaded from meta)', method, url, headers, queryParams, body, auth } })`. Clear the timeout on cleanup and on each field change. Use a `useRef` for the timer ID. Skip the first render (use a `hasLoaded` ref that starts false and flips true after the first `loadFromFile`).
    - **Layout (per D-01):** Flex column, full height:
      - `<UrlBar />` at top
      - HeroUI `<Tabs>` with `variant="underlined"` below the URL bar, with 4 tabs:
        - `<Tab key="params" title="Params">` → `<KeyValueEditor entries={queryParams} onChange={setQueryParams} keyPlaceholder="Parameter" valuePlaceholder="Value" />`
        - `<Tab key="headers" title="Headers">` → `<KeyValueEditor entries={headers} onChange={setHeaders} keyPlaceholder="Header" valuePlaceholder="Value" />`
        - `<Tab key="body" title="Body">` → `<BodyEditor body={body} onChange={setBody} />` (always visible per D-03)
        - `<Tab key="auth" title="Auth">` → `<AuthEditor auth={auth} onChange={setAuth} />`
      - The tab content area should scroll: `overflow-y-auto flex-1`
    - Keep `data-testid="request-editor"` on the root div.
    - Show placeholder text "Select a request from the sidebar" when `activeRequestId` is null, instead of the tabs/URL bar.
  </action>
  <verify>
    <automated>cd /Users/kenneth/Documents/rust/dispatch && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/features/http/UrlBar.tsx contains `Dropdown` and `DropdownMenu` with GET/POST/PUT/DELETE
    - src/features/http/UrlBar.tsx contains `isLoading` prop bound to response loading state
    - src/features/http/UrlBar.tsx contains `sendRequest`
    - src/features/http/KeyValueEditor.tsx contains `Checkbox` for enable/disable toggle
    - src/features/http/KeyValueEditor.tsx contains `+ Add` or `Add` button text
    - src/features/http/KeyValueEditor.tsx contains `onPress` that appends `enabled: true`
    - src/features/http/BodyEditor.tsx contains `<textarea` (not CodeMirror/Monaco)
    - src/features/http/BodyEditor.tsx contains `type: 'json'`
    - src/features/http/AuthEditor.tsx contains `type="password"`
    - src/features/http/AuthEditor.tsx contains `type: 'bearer'`
    - src/components/layout/RequestEditor.tsx contains `<Tabs` with `variant`
    - src/components/layout/RequestEditor.tsx contains `<Tab key="params"`
    - src/components/layout/RequestEditor.tsx contains `<Tab key="headers"`
    - src/components/layout/RequestEditor.tsx contains `<Tab key="body"`
    - src/components/layout/RequestEditor.tsx contains `<Tab key="auth"`
    - src/components/layout/RequestEditor.tsx contains `loadRequest` call in useEffect
    - src/components/layout/RequestEditor.tsx contains `saveRequest` call with debounce (setTimeout)
    - src/components/layout/RequestEditor.tsx contains `useCollectionStore` for activeRequestId
    - src/components/layout/RequestEditor.tsx contains `data-testid="request-editor"`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Request editor renders URL bar with method dropdown and Send button, four tabs (Params/Headers/Body/Auth), key-value editors for params and headers, body textarea, bearer auth input. Loading a request from sidebar populates all fields. Edits auto-save with 500ms debounce. Send button triggers request execution and shows loading state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of request editor</name>
  <files>src/components/layout/RequestEditor.tsx</files>
  <action>Human visually verifies the request editor UI components render and function correctly in the running Tauri app.</action>
  <what-built>Full request editor: URL bar (method dropdown + URL input + Send button), four tabs (Params | Headers | Body | Auth), key-value editors with checkbox toggle and delete, body textarea, auth token input. Connected to requestStore and collectionStore.</what-built>
  <how-to-verify>
    1. Run `npm run tauri dev` in the project root
    2. Create a collection and a request from the sidebar (right-click context menu)
    3. Click the request in the sidebar — verify the editor loads (URL bar appears with method dropdown and Send button)
    4. Click the method dropdown — verify GET, POST, PUT, DELETE options appear
    5. Click the "Params" tab — verify key-value editor appears with checkbox, key/value inputs, delete button, and "+ Add" button
    6. Add a parameter row — verify checkbox is checked by default, inputs are editable
    7. Uncheck the checkbox — verify the row is visually grayed out
    8. Click the "Headers" tab — verify same key-value editor layout
    9. Click the "Body" tab — verify a plain textarea appears (not a code editor)
    10. Click the "Auth" tab — verify a bearer token input field appears
    11. Type a URL (e.g., `https://httpbin.org/get`) and click Send — verify the Send button shows loading state briefly
    12. Verify a response appears in the response viewer below (may show raw if Plan 03 not yet executed)
  </how-to-verify>
  <verify>Human confirms all 12 verification steps pass</verify>
  <done>Request editor UI renders correctly with all tabs, components, and interactions working as specified</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All four tabs render correct content
- Key-value editor rows have checkbox, key input, value input, delete button
- Send button triggers request execution via requestStore
- Active request from sidebar loads into editor fields
- TypeScript compiles cleanly
</verification>

<success_criteria>
- User can select HTTP method from dropdown
- User can enter URL and click Send
- User can add/toggle/delete query params and headers
- User can write JSON body in textarea
- User can set bearer token
- Send button shows loading state during execution
- Switching requests in sidebar loads new request into editor
</success_criteria>

<output>
After completion, create `.planning/phases/03-http-engine/03-02-SUMMARY.md`
</output>
